# SRA-Pro Core Dependencies (Optimized for NVIDIA Lab / DGX A100)

# Core Deep Learning
torch
transformers>=4.48.0
datasets
accelerate>=0.34.0

# QLoRA & Fine-Tuning
peft>=0.12.0
trl>=0.11.0
bitsandbytes>=0.43.0

# Performance & Long Context (Essential for A100/H100)
# Note: flash-attn usually requires: pip install flash-attn --no-build-isolation
# we include it here for standard environments
flash-attn

# Tokenization & Data Handling
sentencepiece
protobuf
scipy
psutil
tqdm

# Monitoring & Speed
wandb
hf_transfer # Set HF_HUB_ENABLE_HF_TRANSFER=1 for 10x faster model downloads
