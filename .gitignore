# Dependencies
node_modules/
.pnp
.pnp.*

# Logs
logs
npm-debug.log*
codeql.json
dependabot.json
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Runtime log files
tmp/*.log
tmp/*.err.log

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Testing
coverage/

# Environment
.env
.env.local

# Backups
/backups

# CLI docker-compose mount point — scoped so the CLI container never sees the repo root
# (backend/.env, frontend/.env, terraform/) instead of only the project it's scanning.
/workspace/*
!/workspace/.gitkeep

# Personal Developer Notes
/COMMANDS/
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/
out/

# Misc
*.pem

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
*.tfplan
tfplan
terraform/terraform.tfstate.1769920708.backup
.vercel
.env*.local

# Platform log exports. Carry request ids, user agents and access patterns for real sessions —
# useful to read while debugging, never something to publish into the repository.
sra-backend-log-export-*.csv
